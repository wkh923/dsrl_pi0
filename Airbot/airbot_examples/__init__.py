"""
Examples for AIRBOT Python SDK.

The examples are shipped with the AIRBOT Python SDK and can
directly run on the command line. Running examples would require
package `airbot_py` to be installed.

**After** launching the AIRBOT driver serivice (see step 1 and
step 2 of [introduction](../#control-airbot-with-python-sdk)),
the examples can be run with the following command:

```bash
python3 -m airbot_examples.<example_name> --port <port>
```

Available arguments for each examples could be examined by

```bash
python3 -m airbot_examples.<example_name> --help
```
"""
